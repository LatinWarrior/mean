var mainAppModuleName = 'myApp';

var mainApplicatonModule = angular.module(mainAppModuleName, ['example']);

// angular.element(document).ready(function(){
//     angular.bootstrap(document, [mainAppModuleName]);
// });

